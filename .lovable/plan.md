

## תוכנית: עדכון אזור הלוגו בסיידבר

### מה צריך לשנות

בקובץ `src/components/layout/Sidebar.tsx`, שורות 152-162:

1. **הסרת הכיתוב "Mlaiko"** — מחיקת ה-`<span>` בשורה 161
2. **פריסת הלוגו המלא** — הלוגו (התמונה שהועלתה) יתפרס על כל רוחב אזור ההדר של הסיידבר, ללא חיתוך או הגדלה מלאכותית. הסרת ה-`scale` וה-`translate` שהוספו קודם, והחלפתם ב-`object-contain` כדי שהתמונה המלאה (האייקון בלבד) תוצג בצורה נקייה ומדויקת בתוך המלבן

### שינויים טכניים

**`src/components/layout/Sidebar.tsx`:**
- הסרת `<span className="...">Mlaiko</span>`
- שינוי ה-`<img>` ל-`object-contain` במקום `object-cover object-top scale-[1.6] translate-y-[10%]`
- התמונה תתפרס על כל האזור הזמין בהדר

