הסרת שורת הטיפ "💡 אפשר לחפש גם לפי קטגוריה או ספק" מתחת לשדה החיפוש בדף המלאי.

**קובץ לעריכה:** `src/components/inventory/MobileSearchBar.tsx`

**שינוי:** מחיקת אלמנט ה־`<p>` עם הטקסט המסומן באדום. שאר הקומפוננטה (שדה החיפוש, האייקון, כפתור הניקוי, התווית) נשארת כמו שהיא.